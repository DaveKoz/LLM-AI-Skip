# LLM-AI-Skip
This repo is a shareable resource between developers developing using Skip tools. There are a lot of little tweaks that you have to do to make everything run correctly and compile correctly in Xcode so that the Android version builds. 
